{posts.map((p) => (
  <div key={p.id} className="card fade-up">
    <p>{p.content}</p>

    <div style={{ marginTop: 10 }}>
      <button onClick={() => handleReply(p.id, 'logic')}>
        Logic galat lag rahi
      </button>

      <button onClick={() => handleReply(p.id, 'next')}>
        Next step kya ho sakta hai
      </button>
    </div>
  </div>
))}
